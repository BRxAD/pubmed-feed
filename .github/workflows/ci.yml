name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - uses: actions/setup-node@v5
        with:
          node-version: "24"
          package-manager-cache: false

      - name: Install dependencies
        run: npm install --legacy-peer-deps

      - name: Typecheck
        run: npm run typecheck

      - name: Production build
        run: npm run build

  smoke-test:
    needs: build
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    steps:
      - name: OpenAlex ingest is disabled (PubMed-only)
        run: |
          url="https://pubmedfeed.vercel.app/api/ingest/openalex?topicName=main"
          echo "GET $url"
          code=$(curl -sS -o /tmp/body.json -w "%{http_code}" "$url")
          echo "HTTP $code"
          cat /tmp/body.json
          test "$code" = "410"
          grep -q '"ok":false' /tmp/body.json

      - name: Check PubMed feed page
        run: |
          code=$(curl -sS -o /dev/null -w "%{http_code}" "https://pubmedfeed.vercel.app/feed?source=pubmed")
          echo "Feed HTTP $code"
          test "$code" = "200"

      - name: Check Brief homepage
        run: |
          code=$(curl -sS -o /dev/null -w "%{http_code}" "https://www.stewardshipbrief.com/")
          echo "Homepage HTTP $code"
          test "$code" = "200"
